#!/usr/bin/env bash

#
# WARNING: this script is auto-generated by Ansible - one might need to change the `domain` variable if the domain name changes
#

domain_name={{ domain }}

function parse_yaml {
   local prefix=$2
   local s='[[:space:]]*' w='[a-zA-Z0-9_]*' fs=$(echo @|tr @ '\034')
   sed -ne "s|^\($s\):|\1|" \
        -e "s|^\($s\)\($w\)$s:$s[\"']\(.*\)[\"']$s\$|\1$fs\2$fs\3|p" \
        -e "s|^\($s\)\($w\)$s:$s\(.*\)$s\$|\1$fs\2$fs\3|p"  $1 |
   awk -F$fs '{
      indent = length($1)/2;
      vname[indent] = $2;
      for (i in vname) {if (i > indent) {delete vname[i]}}
      if (length($3) > 0) {
         vn=""; for (i=0; i<indent; i++) {vn=(vn)(vname[i])("_")}
         printf("%s%s%s=\"%s\"\n", "'$prefix'",vn, $2, $3);
      }
   }'
}

while read -r endpoint
do
  client_public_ip=$(echo ${endpoint}|awk '{print $2}'|cut -d: -f1)
  private_key=$(echo ${endpoint}|awk '{print $1}')
  connected_client=$(parse_yaml {{ outputdir }}/configs.yml|grep ${private_key})
  client_name=$(echo ${connected_client}|cut -d_ -f1)
  client_private_ip=$(parse_yaml {{ outputdir }}/configs.yml|grep ${client_name}|grep cidr|cut -d= -f2|cut -d/ -f1|tr -d '"')
  if [ -n "${client_name}" ] && [ -n "${client_public_ip}" ]
  then
    if [ -f "{{ outputdir }}/hosts.dynamic" ]
    then
      cp {{ outputdir }}/hosts.dynamic {{ outputdir }}/hosts.dynamic.temp
      grep -v "${client_name} ${client_name}.${domain_name}" {{ outputdir }}/hosts.dynamic.temp > {{ outputdir }}/hosts.dynamic
    fi
    echo "${client_public_ip} ${client_name} ${client_name}.${domain_name}" >> {{ outputdir }}/hosts.dynamic
    echo "${client_private_ip} ${client_name} ${client_name}.${domain_name}" >> {{ outputdir }}/hosts.dynamic
  fi
done <<< $(wg show wg-dns-server endpoints|grep -v '(none)')
systemctl restart dnsmasq
